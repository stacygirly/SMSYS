# --- Web & sessions ---
Flask>=3.0.0,<4
Flask-Cors>=4.0.0,<5
Flask-Login>=0.6.3,<0.7
Flask-Session>=0.8.0,<0.9
Flask-PyMongo>=2.3.0,<3
pymongo[srv]>=4.7.0,<5
bcrypt>=4.1.2,<5
gunicorn>=21.2,<22

# --- ML / audio stack ---
# numpy 2.x is required for Python 3.12; librosa 0.10.2.post1 supports it.
numpy>=2.0.0,<3
scipy>=1.11,<2
librosa>=0.10.2.post1,<0.11
numba>=0.60,<1
joblib>=1.3,<2
soundfile>=0.12,<1
SpeechRecognition>=3.10.3,<4

# TensorFlow (CPU). Do NOT add tensorflow-io-gcs-filesystem here.
tensorflow==2.20.0

# --- OpenAI SDK ---
openai>=1.40.0,<2

# --- Optional dev convenience (loads .env locally; Render uses dashboard env vars) ---
python-dotenv>=1.0.1,<2
